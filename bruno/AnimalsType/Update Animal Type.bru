meta {
  name: Update Animal Type
  type: http
  seq: 4
}

patch {
  url: {{URL}}/{{API}}/animal-types/1
  body: json
  auth: inherit
}

body:json {
  {
    // Required (in create)
    "name": "Chèvre"
  }
}
