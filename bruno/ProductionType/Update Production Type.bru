meta {
  name: Update Production Type
  type: http
  seq: 5
}

patch {
  url: {{URL}}/{{API}}/production-type/1
  body: json
  auth: inherit
}

body:json {
  {
    // Required (in create)
    "name": "Viande"
  }
}
