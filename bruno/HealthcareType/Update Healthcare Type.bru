meta {
  name: Update Healthcare Type
  type: http
  seq: 5
}

patch {
  url: {{URL}}/{{API}}/healthcare-types/1
  body: json
  auth: inherit
}

body:json {
  {
    // Required (in create)
    "name": "Traitements antiparasitaires"
  }
}
